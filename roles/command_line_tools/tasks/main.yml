---
- name: Install command-line tools
  package:
    name: "{{ item }}"
  with_items:
    - "@Development tools"
    - autoconf
    - automake
    - cmake
    - git
    - libtool
    - nano
    - ninja-build
    - scons
    - valgrind
  become: true
