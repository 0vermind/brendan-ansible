---
- package:
    name: "{{ item }}"
  with_items:
    - "@Development tools"
    - autoconf
    - automake
    - cmake
    - git
    - libtool
    - ninja-build
    - scons
    - valgrind
  become: true
